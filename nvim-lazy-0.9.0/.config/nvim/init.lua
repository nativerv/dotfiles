require 'user.bootstrap' -- Bootstrap (download) `lazy.nvim` plugin manager
require 'user.options' -- Customize built-in nvim settings
require 'user.mappings' -- Customize built-in nvim mappings
require 'user.autocmds' -- Autocmds that do different handy things, 
                        -- all still only using built-in nvim stuff 
require 'user.plugins' -- Install and configure plugins with `lazy.nvim`

