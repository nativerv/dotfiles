# The following should be considered catch-all directories
# The rest is blacklisted by stock disable-xdg.inc
blacklist ${DESKTOP}
blacklist ${DOWNLOADS}

# Disable my custom ${HOME} dirs too
blacklist ${HOME}/vms
blacklist ${HOME}/dock
blacklist ${HOME}/pr
read-only ${HOME}/dot
blacklist ${HOME}/edu
blacklist ${HOME}/pub

# vim:syntax=firejail
